

Scene 10:
- The algorithm box shows up too soon. Add a highlight step on title text "Recursive Partitioning"
- The texts inside the algorithm box must have proper indentation and syntax highlighting as if it's code
- Before showing "Measure purity by Entropy", first show "Goal of split: create pure subsets". I've made changes to narration texts in the scene file to better align with the narrative
- Do not shrink the algorithm when talking about tree pruning, keep it where it is;
- Move the two pruning visualization closer to each other to occpy the space on the right side of the screen
- For post-pruning, need to use the scissor icon to cutoff nodes, not just make some decision nodes disappear

Scene 11:
- When talking about model evaluation for classification vs. numeric prediction, first show "Classification" and "Numeric Prediction" titles, and only show the details of each when the narrative is talking about it
- The confusion matrix column titles are overlapping with each other; the labels that should be within each cell (such as "True Positives") are current mis-placed or outside of cells, and the highlighting is out of sync with the narrative

Scene 12:
- The confusion matrix column titles are overlapping with each other
- The placement of accuracy, precision, recall, and F-measure are all over the place. Do not shrink any, just place them one after another and sync with narrative.
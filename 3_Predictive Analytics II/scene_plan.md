# Scene 1

**Text**: In this video, you'll dive into classification and learn about predicted probabilities, a new classification algorithm called naive Bayes, and cost-sensitive classification.

**Visual**: The title "Predictive Analytics" in center screen. Below it, subtitle "Predicted Probabilities, Naive Bayes, and Cost-Sensitive Classification".

# Scene 2

**Text**: From the last video, we've already learned two classification techniques, k-nearest-neighbors and the decision tree. Although they are very different models, they share an important characteristic: both k-NN, the decision tree, and in fact most other classification techniques can produce not just a class prediction, but also the probability of a data point belonging to a certain class. In a binary classification problem, for example, these methods can predict not only whether a data point belongs to one class or the other, but also the probability that it belongs to each class. A higher predicted probability means the model is more confident in its prediction.

For k-NN and the decision tree, obtaining a class probability is straightforward. In k-NN, the percentage of a given class among the k neighbors is the predicted probability of that class. In the decision tree, the percentage of a given class among the data in the leaf node that the data point falls into is the predicted probability. 

Discrete class predictions are actually generated from these probabilities using a cutoff value. Take binary classification as an example: the cutoff is usually 0.5 by default. If the probability of belonging to class 1 is greater than 0.5, the data point is predicted to be in class 1; otherwise, it's predicted to be in class 0. This is equivalent to a majority vote: the majority class is, by definition, the class with more than 50% proportion.

**Visual**: Title "Probability Predictions from k-NN and Decision Tree" in center top. Below it, re-use the 3-NN visualization and decision-tree visualization from scenes 07 and 09 of the "Predictive Analytics I" unit, but make them smaller and align side by side. 

Under the 3-NN visual, tally converting the class proportion into a probability bar. Under the Decision Tree, highlight a leaf node box showing class counts converting into its own probability bar. Both probability bars feed into a shared number line from 0 to 1 with a cutoff marker at 0.5; animate a point sliding just above and then just below the cutoff, flipping the predicted label between "Class 1" and "Class 0", with a label "= majority vote" appearing beside the cutoff.

# Scene 3

**Text**: More generally, a classification task can be thought of as predicting class probabilities, and then picking the class with the highest probability as the prediction. Suppose each data point X is represented by a vector of features, and there are m possible classes, C1 through Cm. Our goal is to estimate the conditional probability of class Ci given the features X — in other words, given X, what's the probability that it belongs to class Ci? k-NN and the decision tree estimate this conditional probability using simple proportions, and then predict whichever class has the highest estimated probability. This is the logic that k-NN, the decision tree, and many other classifiers share.

Naive Bayes follows the same logic in making predictions — pick the class with the highest probability — except that it uses Bayes' theorem to estimate those class probabilities.

**Visual**: Title "Classification as a Probability Prediction" in center top. Below it, show text "Data point X = (x_1, ..., x_n); Classes (C_1, ..., C_m)" as notation setup. Below that, in the center of the remaining screen, show a box and build progressively inside it as on-screen text: "Classification = estimate P(Ci | X) for each class → predict the class with highest probability." Around the box, show small visuals for "k-NN" and "Decision Tree" (shrunk down versions of the k-NN and decision tree visuals from previous scene) both point to the box, labeled "estimated via simple proportions." Then a third icon, "Naive Bayes," appears pointing into the same box, initially labeled with a "?" that transforms into "Bayes' Theorem" as the narration names it, setting up the next scene.

# Scene 4

**Text**: You've probably seen Bayes' theorem before in a statistics or probability course — it relates the conditional probabilities of two random variables. Here's a fun puzzle that will give you the basic idea of Bayes' theorem. Suppose there are two bowls of cookies. Bowl A has 30 vanilla cookies and 10 chocolate cookies; Bowl B has 20 of each. You pick one of the two bowls at random, so before seeing anything else, each bowl has a 50% chance of being the one you picked. Then, without looking at which bowl you chose, you reach in and pull out a cookie — it's vanilla. Does this change how likely you think it is that you picked Bowl A versus Bowl B? Intuitively, yes: vanilla cookies make up 75% of Bowl A but only 50% of Bowl B, so drawing a vanilla cookie should make you lean more toward Bowl A. Bayes' theorem gives us a precise way to make this update: it combines what we believed before seeing the cookie — a 50-50 split on the bowl — with how consistent that evidence is with each possibility, to arrive at an updated belief about which bowl we actually picked.

Let's actually work out the number. Before drawing, the probability of Bowl A is 0.5 — that's our prior. Given Bowl A, the probability of drawing a vanilla cookie is 0.75 — that's the likelihood. Multiplying them gives 0.5 times 0.75, which is 0.375. We do the same for Bowl B: prior 0.5, likelihood of vanilla 0.5, giving 0.5 times 0.5, or 0.25. Adding these two together, 0.375 plus 0.25, gives 0.625 — the overall probability of drawing a vanilla cookie, regardless of which bowl it came from. Finally, to get the updated probability that we picked Bowl A given that we saw a vanilla cookie, we divide Bowl A's piece, 0.375, by the total, 0.625, which comes out to 0.6. So after seeing the vanilla cookie, we should believe there's a 60% chance we picked Bowl A — up from the 50% we believed before.

**Visual**: Title "A Quick Refresher: Bayes' Theorem" in center top. Below it, draw Bowl A and Bowl B side by side, each filled with small colored dots representing cookies — Bowl A with 30 tan (vanilla) and 10 brown (chocolate) dots, Bowl B with 20 tan and 20 brown dots. Above each bowl, show "50%" with an arrow, labeled "which bowl did we pick?" Then animate a hand icon reaching into an unmarked/blurred bowl and pulling out a single tan (vanilla) dot, enlarged in the center. Below each bowl, build the fraction of vanilla cookies: "75%" under Bowl A and "50%" under Bowl B, labeled "how likely is a vanilla cookie from this bowl?"

Then transition to a worked calculation beneath the bowls, building Bayes' formula term by term for Bowl A: "P(A | vanilla) = [P(vanilla | A) × P(A)] / P(vanilla)". As each symbolic term appears, substitute its number directly below it and label it: "0.5" under P(A) labeled "prior," "0.75" under P(vanilla | A) labeled "likelihood." Multiply these to show "0.375" appearing in the numerator. Do the same in a smaller, dimmed row for Bowl B, producing "0.25." Add the two together sliding into the denominator to form "0.625," labeled "P(vanilla) — total probability, both bowls." Finally divide 0.375 by 0.625, revealing "0.6" which lands inside a highlighted box labeled "P(A | vanilla) = 60% — posterior." Show the two probability bars from before now landing at their exact heights, 60% for Bowl A and 40% for Bowl B, confirming the "more likely Bowl A" intuition with the precise number.

# Scene 5

**Text**: Now back to the classification task. We want the conditional probability of being in class Ci given the features X. Bayes' theorem lets us express that as the conditional probability of observing features X given class Ci, multiplied by the unconditional probability of being in class Ci, divided by the unconditional probability of observing X.

The probability of Ci given X is the posterior probability. The probability of X given Ci is the likelihood. And the unconditional probability of Ci is the prior probability. The prior describes what we know about the class distribution before seeing any specific data point; the likelihood describes how likely it is to observe a data point like X given a particular class; together they combine into the posterior, the class probability conditional on the data.

Going back to the classification task, our goal is to find the class that maximizes the posterior probability — which is the same as maximizing the product of likelihood and prior. Notice we don't need the denominator, the probability of observing X, at all: its value is the same constant regardless of which class we're considering, so we don't need to calculate it.

**Visual**: Title "Naive Bayes: Bayes' Theorem for Classification" in center top. Under it, build the Bayes' theorem formula term by term. P(Ci | X) appears first, labeled "posterior." Then "=" and the right-hand side build in: P(X | Ci) labeled "likelihood," times P(Ci) labeled "prior," all over P(X) labeled "evidence." As each term is named in the narration, highlight just that term in yellow while the rest dims. At the end, gray out and cross the denominator P(X) with a label "same for every class — ignore it," while the numerator (likelihood × prior) is boxed and highlighted as "what we actually need to maximize."

# Scene 6

**Text**: So how do we calculate the pieces we need, starting with the prior class probabilities? One straightforward, data-driven way to get the probability of class Ci is to estimate it from the training data: take the number of data points in Ci and divide by the total size of the training data. This approximates the probability of being in Ci regardless of feature values. If training data is somehow insufficient or unreliable, people sometimes also estimate this from domain knowledge, or simply assume a uniform probability across classes.

Next, how do we estimate the likelihood — the probability of observing X given class Ci? By definition, we could estimate it by counting the fraction of data points with exactly features X among those in class Ci. But when there are many features, this becomes computationally very costly, because we'd need to locate one very specific feature vector among many many possible combinations.

To deal with this, Naive Bayes makes a "naive" assumption — class-conditional independence: conditional on the class label Ci, the features are assumed independent of each other. This lets us rewrite the probability of the whole feature vector X given Ci as the product of the conditional probabilities of each individual feature given Ci. The assumption is unrealistic in many settings — this is actually where Naive Bayes gets its name — but it turns an exponentially complex computation into a simple multiplication of individual probabilities, each of which is easy to estimate from data.

Now the question of estimating the likelihood term becomes how do we estimate the conditional probability of one specific feature value, xk, given class Ci. If the feature is categorical, this is easy: we simply find the fraction of data points in class Ci that have feature value xk.

One issue to watch for: what if that fraction happens to be zero — no data points in class Ci have feature value xk? Then the entire likelihood calculation, which is a product, collapses to zero. The fix is called Laplace correction: we simply add small non-zero values to both the numerator and denominator of the fraction.

If the feature is numerical, we can't compute a simple fraction, since it can take infinitely many possible values. In that case, we either discretize the feature and treat it as categorical, or assume it follows some distribution and use that distribution's density function to compute the conditional probability.

**Visual**: Title "Naive Bayes: Calculation Details" in center top. Under it on the left side: a small training dataset table (rows of data points, a few feature columns titled "X_1", "X_2", etc., and a "Class" column). Right side: show the same Naive Bayes formula from last scene (with denominator greyed out). When talking about prior calculation, highlight the prior term and at the same time show a bracket around the Ci rows and the formula count(Ci) / total building beside it. When talking about likelihood calculation, highlight the likelihood term and  the feature vector X = (x1, x2, ..., xk) drawn as a row of boxes. First show the "exact-match" counting idea as a branching tree of exponentially many possible feature combinations, visually overwhelming, crossed out in red. Then transforms P(X|Ci) term into the factorized form P(x1|Ci) × P(x2|Ci) × ... × P(xk|Ci) with a label "Class-Conditional Independence" next to it, each small term appearing under its corresponding feature box.

Next, focus on one feature box xk. Show the categorical case as a small labeled dataset filtered to class Ci, counting matches of xk into a fraction count(xk, Ci) / count(Ci). Animate that fraction hitting zero (a large red "0"), then small "+" terms sliding into both numerator and denominator, labeled "Laplace correction," turning the zero into a small nonzero value. Below, for the numerical case, show a continuous number line for xk either snapping into discrete bins (discretization) or with a smooth density curve drawn over it (distributional assumption), with the curve's height at xk highlighted as the likelihood value.

# Scene 7

**Text**: I want to conclude the discussion of Naive Bayes on two remarks. First, Naive Bayes has a property that not every predictive technique enjoys: having irrelevant features in the data doesn't hurt its predictive performance. Imagine x0 is an irrelevant feature — one that carries no useful information for prediction. Then the conditional probability of x0 given class Ci should be roughly the same across all classes, so multiplying it into the likelihood doesn't change which class has the highest probability, and it doesn't affect the prediction.

Second, the class-independence assumption can make Naive Bayes' probability predictions inaccurate, but the rankings of those probabilities are often still reasonably good. This is a big part of why Naive Bayes still works well in practice despite the unrealistic assumption behind it.

**Visual**: Title "Naive Bayes: Calculation Details" in center top. Under it, show two bullet points for the two remarks on the left side of the screen. For the first remark, show the factorized product from previous scenes on the right side of the screen and then append an extra term P(x0 | Ci). Animate that term's value as nearly identical small bars across classes C1, C2, C3, then show it factoring out of the argmax comparison without changing which class wins — the winning class stays highlighted before and after x0 is added. For the second remark, show a probability bar chart where the predicted bars are visibly off from a faint "true probability" reference chart behind them, but the order of the bars (tallest to shortest) still matches, labeled "ranking preserved."

# Scene 8

**Text**: Having probability predictions from a classification model not only allows you to obtain class predictions, it also creates new applications with practical importance. Here, I want to introduce one such application: cost-sensitive classification. 

In many real-world problems, different types of prediction errors carry different costs. In cancer detection, for example, misclassifying a cancerous case as healthy — a false negative — can be extremely costly: the patient misses out on early treatment while the disease continues to progress. Misclassifying a healthy case as cancerous — a false positive — is comparatively less dangerous: it typically just leads to some extra follow-up testing, which is stressful and inconvenient but nowhere near as costly as missing an actual cancer.

Because of this asymmetry, our goal in classification is sometimes not to maximize accuracy but to minimize the cost of misclassifications — and a simple way to do that is to leverage the cutoff value on the predicted class probabilities. Suppose class 1 is "cancer" and class 0 is "healthy," and misclassifying class 1 as class 0 is far more costly than the reverse, so we want the model to be more conservative when predicting class 0 — we want it to be very sure before predicting "healthy," since being wrong there is expensive. We can do this by lowering the cutoff for classifying a point as class 1, which is equivalent to raising the cutoff for classifying it as class 0. This means we risk misclassifying some healthy cases as cancerous, in order to avoid the far more costly mistake of missing an actual cancer.

If we know the cost of each type of misclassification, we can quantify this directly: assign a cost to false positives and false negatives respectively, sum the cost across all misclassified points, and divide by the total number of data points to get the average misclassification cost. In a cost-sensitive classification task, we then pick the cutoff value that minimizes this average cost on the validation data.

**Visual**: Title "Probability Prediction Application: Cost-Sensitive Classification" in center top. Under it, open on the cancer-detection scenario: two patient-case icons, "Cancerous" and "Healthy," each with an arrow to its wrong-prediction outcome and a cost tag — a large cost tag on "Cancerous predicted as Healthy" (false negative) and a small cost tag on "Healthy predicted as Cancerous" (false positive), making the asymmetry visually obvious. Then bring back the probability number line and cutoff marker from scene 1, sliding the class-1 cutoff leftward (equivalently raising the class-0 cutoff), with a couple of data points near the old cutoff visibly flipping from "predicted class 0 (healthy)" to "predicted class 1 (cancer)." Finally, show the average-cost formula (sum of costs ÷ number of data points) plotted as a curve against different cutoff values, with a marker dropping to the cutoff that minimizes the curve, labeled "chosen on validation data."
